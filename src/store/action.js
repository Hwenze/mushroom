const initData = {
    history: window.location,
}

const calculate = (state = initData, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export { calculate }